import React from "react";

import { observer } from "mobx-react-lite";

import "./Positions.scss";

const Positions: React.FC<any> = () => {
  return <div>Должности</div>;
};

export default observer(Positions);
